<Window x:Class="MdViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mdxaml="clr-namespace:MdXaml;assembly=MdXaml"
        mc:Ignorable="d"
        Title="MdViewer" Height="600" Width="900"
        KeyDown="Window_KeyDown">
    <Grid Margin="20,10">
        <mdxaml:MarkdownScrollViewer x:Name="MarkdownViewer"
                                      Markdown=""
                                      VerticalScrollBarVisibility="Auto"
                                      >
            <mdxaml:MarkdownScrollViewer.MarkdownStyle>
                <Style TargetType="FlowDocument" BasedOn="{x:Static mdxaml:MarkdownStyle.Standard}">
                    <Setter Property="FontFamily" Value="Malgun Gothic, Segoe UI"/>
                    <Setter Property="FontSize" Value="18"/>
                    <Setter Property="PagePadding" Value="10"/>
                    <Style.Resources>
                        <Style TargetType="Paragraph">
                            <Style.Triggers>
                                <Trigger Property="Tag" Value="Heading1">
                                    <Setter Property="FontSize" Value="28"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Margin" Value="0,16,0,12"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Heading2">
                                    <Setter Property="FontSize" Value="26"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Margin" Value="0,14,0,10"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Heading3">
                                    <Setter Property="FontSize" Value="24"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Margin" Value="0,12,0,8"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Heading4">
                                    <Setter Property="FontSize" Value="22"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Margin" Value="0,10,0,6"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Heading5">
                                    <Setter Property="FontSize" Value="20"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Margin" Value="0,8,0,4"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Heading6">
                                    <Setter Property="FontSize" Value="18"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#666666"/>
                                    <Setter Property="Margin" Value="0,6,0,4"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="CodeBlock">
                                    <Setter Property="FontFamily" Value="D2Coding, GulimChe, DotumChe, Consolas, Courier New"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Padding" Value="10"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="Run">
                            <Style.Triggers>
                                <Trigger Property="Tag" Value="CodeSpan">
                                    <Setter Property="FontFamily" Value="D2Coding, GulimChe, DotumChe, Consolas, Courier New"/>
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Style.Resources>
                </Style>
            </mdxaml:MarkdownScrollViewer.MarkdownStyle>
        </mdxaml:MarkdownScrollViewer>
    </Grid>
</Window>
